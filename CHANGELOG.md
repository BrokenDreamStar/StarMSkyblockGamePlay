# 更新日志

本项目的所有显著变更都会记录在此文件中。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [1.0.1] - 2026-08-07

### 新增

- **滴水石锥加速生长**：对符合条件的钟乳石结构定期触发随机刻，生长速度约提升至原版的 100 倍（约 44 秒/格，原版约 100 分钟/格）
  - 由原版逻辑驱动生长，保持原版生长规则（水源条件、7 格长度上限等）
  - 顺带加速钟乳石尖随机刻的其他效果：炼药锅填充（可再生水/熔岩农场）、泥巴 → 黏土转化
  - 可通过 `random-ticks-per-pass` 与 `check-interval-ticks` 调节生长速度
  - 通过放置事件索引维护钟乳石结构，启动时自动补建、周期性纠偏

### 性能修复

- 滴水石锥加速功能由"全量区块扫描"改为**事件驱动索引**（放置事件建索引 + 启动补建 + 周期纠偏），消除了主线程逐方块遍历开销（实测约占主线程 19% 时间 → 降至可忽略）

### 配置变更

- `dripstone-growth` 配置段调整：
  - 新增 `check-interval-ticks`（触发轮间隔，默认 100 tick = 5 秒）
  - 新增 `rescan-interval-minutes`（纠偏扫描间隔，默认 10 分钟，0=关闭）
  - 新增 `scan-chunks-per-tick`（全量扫描每 tick 限量）
  - 移除 `chunks-per-tick`（不再需要全量扫描限速）

### 变更

- 插件版本号提升至 1.0.1
