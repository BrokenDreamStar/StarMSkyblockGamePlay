# 更新日志

本项目的所有显著变更都会记录在此文件中。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [1.0.3] - 2026-08-21

### 新增

- **水中快速氧化**：当铜方块及其变种、铜傀儡在水中时加速其氧化
  - 铜方块：未上蜡、未满氧化的铜家族方块（铜块、切制铜、台阶/楼梯、活板门、门、栅栏、
    锁链、灯、栏杆、铜储物箱、避雷针等）在水中（自身水浸或轴向邻接水）时，每隔
    `check-interval-ticks`（默认 600 tick = 30 秒）对其调用**真实 `randomTick()`** 一次
    （`random-ticks-per-pass`，默认 1），由原版预氧化/晋级逻辑驱动；孤立湿铜平均约
    10 分钟氧化一阶段
  - 铜傀儡：未被上蜡的铜傀儡在水中时，平均每 `golem-random-tick-interval-seconds`
    （默认 900 秒 = 平均约 15 分钟）被随机刻选中一次，选中即氧化一阶段（实体无 randomTick，按抽取模型）
  - 上蜡的方块/铜傀儡不会氧化；已完全氧化的不再有下一阶段
  - 通过放置/水流/破块事件增量维护“湿铜”索引，启动全量补建 + 周期性纠偏，避免持续全量扫描
  - 新增配置段 `copper-oxidation`（`enabled` / `worlds` / `check-interval-ticks` /
    `random-ticks-per-pass` / `golem-random-tick-interval-seconds` /
    `rescan-interval-minutes` / `scan-chunks-per-tick`）

### 变更

- 插件版本号提升至 1.0.3

## [1.0.2] - 2026-08-21

### 新增

- **精灵球返还**：增强精灵球未命中生物、或命中无法捕捉的生物（无刷怪蛋、被白名单/黑名单排除、概率为 0）时，将精灵球返还给投掷者
  - 返还的精灵球保留原自定义捕捉概率与物品名称/Lore
  - 背包已满时掉落在玩家脚下
  - 可通过 `snowball-converter.refund-on-miss` 开关（默认开启）
  - 新增消息键：`snowball-converter.miss-returned` / `no-egg-returned` / `cannot-catch-returned`
- **刷怪笼刷怪蛋强化**：对同一个普通刷怪笼反复使用刷怪蛋，每个蛋都提升一点刷怪效率（无等级系统）
  - 线性模型：喂满 `max-eggs`（默认 30）个刷怪蛋时，刷怪 CD 缩短为原版的 1/`max-multiplier`（默认 5）
  - 效率提升同时作用于 **缩短刷怪 CD**（最小/最大延迟除以倍率）与 **增加每次刷出数量**（统一从 3 只起步，每 5 个蛋 +1 只，可在 `base-spawn-count` / `eggs-per-extra-mob` 调整），并放宽附近实体上限
  - **只允许使用与刷怪笼已有生物匹配的刷怪蛋强化**；使用不匹配的刷怪蛋时，对已有强化的刷怪笼先警告“会清空强化”，需**再次右键同种蛋确认**后才执行原版替换并把强化重置为原版（已喂蛋数归零、效率还原 1 倍），避免误操作与免费继承已加满的效率
  - 空手右键刷怪笼（查询）或用匹配刷怪蛋喂养时，都在**动作栏**显示已放入的刷怪蛋数量与当前效率
  - 已喂蛋数与基准刷怪参数持久化在刷怪笼方块的 PDC 中，服务器重启与精准采集搬运均保留
  - 新增消息键：`spawner-upgrade.fed` / `status` / `confirm-warning` / `max-reached` / `replaced`

## [1.0.1] - 2026-08-07

### 新增

- **滴水石锥加速生长**：对符合条件的钟乳石结构定期触发随机刻，生长速度约提升至原版的 100 倍（约 44 秒/格，原版约 100 分钟/格）
  - 由原版逻辑驱动生长，保持原版生长规则（水源条件、7 格长度上限等）
  - 顺带加速钟乳石尖随机刻的其他效果：炼药锅填充（可再生水/熔岩农场）、泥巴 → 黏土转化
  - 可通过 `random-ticks-per-pass` 与 `check-interval-ticks` 调节生长速度
  - 通过放置事件索引维护钟乳石结构，启动时自动补建、周期性纠偏

### 性能修复

- 滴水石锥加速功能由"全量区块扫描"改为**事件驱动索引**（放置事件建索引 + 启动补建 + 周期纠偏），消除了主线程逐方块遍历开销（实测约占主线程 19% 时间 → 降至可忽略）

### 配置变更

- `dripstone-growth` 配置段调整：
  - 新增 `check-interval-ticks`（触发轮间隔，默认 100 tick = 5 秒）
  - 新增 `rescan-interval-minutes`（纠偏扫描间隔，默认 10 分钟，0=关闭）
  - 新增 `scan-chunks-per-tick`（全量扫描每 tick 限量）
  - 移除 `chunks-per-tick`（不再需要全量扫描限速）

### 变更

- 插件版本号提升至 1.0.1
